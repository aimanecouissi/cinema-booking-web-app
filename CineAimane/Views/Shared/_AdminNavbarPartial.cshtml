<header>
	<nav class="navbar navbar-expand-lg bg-white fixed-top border-bottom py-3">
		<div class="container">
			<a class="navbar-brand fw-bold me-lg-4" asp-controller="Home" asp-action="Index"><i class="fa-solid fa-clapperboard me-2 text-primary"></i>CineAimane</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li class="nav-item">
						<a class="nav-link @(Context.Request.Path.ToString().ToLower().EndsWith("add") ? "active" : "")" asp-controller="Admin" asp-action="Add">Add</a>
					</li>
					<li class="nav-item">
						<a class="nav-link ms-lg-3 @(Context.Request.Path.ToString().ToLower().EndsWith("movies") ? "active" : "")" asp-controller="Admin" asp-action="Movies">Movies</a>
					</li>
					<li class="nav-item">
						<a class="nav-link ms-lg-3 @(Context.Request.Path.ToString().ToLower().EndsWith("account") ? "active" : "")" asp-controller="Admin" asp-action="Account">Account</a>
					</li>
					<li class="nav-item">
						<a class="nav-link ms-lg-3" asp-controller="Admin" asp-action="Logout">Logout</a>
					</li>
				</ul>
			</div>
		</div>
	</nav>
</header>